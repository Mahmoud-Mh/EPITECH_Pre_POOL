# Add an element at the beginning of the list and display all its elements
numbers = [10, 20, 30, 40, 50]
numbers.insert(0, 5)  # Inserts 5 at the start
print("After adding a number at the beginning, the list is:", numbers)
