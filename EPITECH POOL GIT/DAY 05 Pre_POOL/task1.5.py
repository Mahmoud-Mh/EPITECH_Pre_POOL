# Delete the last element and display the remaining elements
numbers = [10, 20, 30, 40, 50]
if numbers:  # Again, check if the list is not empty
    numbers.pop()  # Removes the last element
    print("After removing the last number, the list is now:", numbers)
