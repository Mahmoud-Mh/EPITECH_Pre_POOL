# Display one element out of two from the list
numbers = [10, 20, 30, 40, 50]
every_second = numbers[::2]  # Slicing to get every second element
print("Every second number from the list:", every_second)
